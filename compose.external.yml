services:
  app:
    depends_on: []
    environment:
      API_DRY_RUN: ${API_DRY_RUN:-false}
      DATABASE_URL: ${DATABASE_URL:?Set DATABASE_URL to your external MySQL address before starting the app}
