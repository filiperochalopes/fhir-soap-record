services:
  app:
    build:
      context: .
      target: deps
    command: sh ./scripts/dev-with-prisma.sh
    environment:
      CHOKIDAR_USEPOLLING: "true"
    volumes:
      - .:/app
      - /app/node_modules
