services:
  app:
    build:
      context: .
      target: deps
    command: sh -lc "corepack enable && pnpm prisma generate && pnpm dev --host 0.0.0.0 --port 3000"
    environment:
      CHOKIDAR_USEPOLLING: "true"
    volumes:
      - .:/app
      - /app/node_modules
