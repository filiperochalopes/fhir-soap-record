services:
  db:
    volumes:
      - mysql_data:/var/lib/mysql

  app:
    build:
      context: .
      target: deps
    command: sh ./scripts/dev-with-prisma.sh
    environment:
      CHOKIDAR_USEPOLLING: "true"
    volumes:
      - .:/app
      - /app/node_modules

volumes:
  mysql_data:
